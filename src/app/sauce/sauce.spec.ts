/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Sauce} from './sauce';

describe('Sauce', () => {
  it('should create an instance', () => {
    expect(new Sauce()).toBeTruthy();
  });
});
